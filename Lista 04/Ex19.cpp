#include <stdio.h>
#include <stdlib.h>
#include <locale.h>
main(){
	char s;
	float a;
	setlocale(LC_ALL,"Portuguese");
	
	for(int c = 1; c <= 50; c ++){
	printf("Digite o seu sexo : (m/f)");
	scanf(" %c",&s);
		if(s == "m" || s == "M"){
			
		}
		else if(){
			
		}
	}
	
	
}

